language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

# CI 실행 완료시 메일로 알람!!
notifications:
  email:
    recipients:
      - kalkin2@naver.com

before_deploy:
  - zip -r modernWeb *
  - mkdir -p deploy
  - mv modernWeb.zip deploy/modernWeb.zip

deploy:
  - provider: s3
    access_key_id: AKIAZQQM76XC4JN562G2
    secret_access_key: 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
    bucket: modern-web
    region: ap-northeast-2
    skip_cleanup: true
    acl: private
    local_dir: deploy
    wait-until-deployed: true